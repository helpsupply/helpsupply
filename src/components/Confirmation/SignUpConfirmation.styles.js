import { css } from 'lib/utils/media-queries';
import { Color } from 'lib/theme';

export const styles = {
  description: css({
    strong: {
      color: Color.PRIMARY,
      fontWeight: 'normal',
    },
  }),
};

export default styles;
