import { css } from '@emotion/core';
import { Color } from 'lib/theme';

export const styles = {
  subRow: css({
    alignItems: 'baseline',
    color: Color.GRAY_75,
    display: 'flex',
    justifyContent: 'space-between',
    width: '100%',
  }),
};
